{
	"name": "apihono-todo-cf",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"deploy:cf": "wrangler deploy --name apirest-todo --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"hono": "^4.9.8"
	},
	"devDependencies": {
		"wrangler": "^4.40.2"
	}
}